Branch 
    InputAction 동적 로딩 기능 구현

Commit 7fcddb3
[Refactor] Input / UI / Game Flow 구조 정리 및 입력 캡처 안정화

요약
입력 시스템(InputManager)과 UI 실행 구조(UIManager), 그리고 게임 흐름(GameManager)의 입력 처리 전환 로직을 재정비
기존 입력 캡처 및 UI 모드 전환 시 발생하던 불안정 요소를 제거, Restricted 모드를 신설
팝업 및 리바인드 캡처 상태를 명확히 분리했다.

상세 수정 내용
1. InputManager.cs
    AllocateInputActions()
        ActionMap 재구성: Player / UI / Title / Locked / Restricted

    각 맵별 액션 분기 정리
        Player → PlayerController.Execute()
        UI / Restricted → UIManager.Execute() 또는 Execute_Internal()
        Title → PressAnyKey.Execute()
        FindAnyObjectByType<PlayerController>(IncludeInactive)로 탐색 방식 변경
        hasAllocated 플래그 및 맵 단위 디버그 로그 추가
    
    입력 모드 전환(ChangeInputMode)
        DeactivateInput → Disable → SwitchCurrentActionMap → Reset → Enable → ActivateInput 순서로 안정화

    새로운 입력 모드 Restricted 추가

    캡처 로직 개선
        InitializeCaptureAction()에서 performed 핸들러 재연결
        이미 활성 상태일 경우 자동 Disable()
        EndCapture()는 기존 로직 유지 (Disable + ResetBindingOverrides)

2. GameManager.cs
    C_GetKeyInfo() 코루틴 정리
    팝업 활성화 시 → InputMode.Restricted로 전환
    입력 1회 캡처 후 → EndCapture() → InputMode.UIOnly 복귀
    팝업 갱신은 UIManager.instance.RefreshUI()로 통일

UI 실행 경로 통일
    UIManager.Execute() / Execute_Internal() 구조 유지
    Menu / Scroll → Internal, 
    나머지 → 일반 Execute

3. InputActions(.inputactions)
    Restricted ActionMap 추가
    각 액션별 groups 및 path 업데이트 (Keyboard, Gamepad 분기 갱신)
    Press/Hold 등 Interaction 속성 정리

4. 기타
    널 가드 및 로깅 정리
    코드 전체 포맷 정리 및 중복 제거

결과
    팝업 캡처 중 입력 차단이 확실히 분리됨
    ActionMap 전환 시 불필요한 중복 실행 제거
    Player / UI / Title 입력 흐름이 독립적으로 안정화됨
    InputActionAsset 재로드 시에도 Binding 상태 일관성 유지

향후 계획
    Execute / Execute_Internal 통합 여부 재검토
    InputActionAsset.LoadBindingOverridesFromJson() 기반 리바인드 복원 방식 전환
    원본 InputActionAsset 보호를 위한 Instantiate() 사용 고려
    캡처 단계에서 “모든 버튼 릴리즈 대기” 루틴 필요 시 별도 유틸로 분리


Commit c904c0b
[Feat] 리소스 동적 교환 기능 구현

요약

리소스 매니저(ResourceManager)에 입력 장치별 동적 리소스 교체 시스템 추가.
입력 장치(Keyboard / Gamepad 등)의 실시간 변경에 따라 대응 리소스(스프라이트)를 자동 전환하도록 확장.
Control 이미지셋은 Multiple → Single Sprite 구조로 변경.

상세 수정 내용

1. Types.cs
    InputMode.None 추가 (비활성 상태 명시용)

2. ResourceManager.cs
    필드 추가
    private) readonly Dictionary<string, string> controlPaths
        입력 컨트롤 경로 관리
        "Keyboard&Mouse", "Gamepad_Xbox", "Gamepad_PS" 등 매핑
    
    함수 추가
    Public) void ChangeResource(string dictionaryName, string ResourceTarget)
        dictionaryName에 지정된 리소스(예: controlSprites)를
        ResourceTarget 경로로 교체/병합
        Gamepad 계열일 경우 General → 변종(Xbox/PS/Switch) 순서로 로드 및 덮어쓰기
        기존 라우트 기준으로 증분 병합(diff) 수행

        필요할 경우 전체 클리어/언로드

3. InputManager.cs
    플레이어 입력 장치 변경 감지 기능 확장
    private) void OnDeviceChanged(PlayerInput pi)
        입력 장치 변경 시 리소스 매니저에 교체 요청
        (ResourceManager.ChangeResource("controlSprites", currentControlScheme))
        동일 스킴 반복 호출 방지 (currentActivatedDevice 비교)

결과
    입력 장치 변경 시 UI 리소스 자동 교체
    Gamepad 사용 시 General + 변종 세트 병합 로드
    기존 리소스 구조를 유지하면서도 장치 기반 동적 로딩 가능

향후 계획
    교체된 리소스를 UI에 반영
    가져온 리소스를 Control UI가 활성화 될때 참조하여 갱신 / Refresh함수 작업    


Commit 59bbdae
[Feat] Control UI ↔ ResourceManager 연동 및 입력 스프라이트 매핑 구조 구현
요약

Control UI와 리소스 매니저의 리소스 요청 경로 확립
이제 UI는 바인딩 변경사항에 대해 실시간으로 스프라이트를 변경을 적용할 수 있습니다.

1. ResourceManager.cs
    Public) sprite GetSprite(string key)
        입력 바인딩에 따른 스프라이트를 전달하기 위한 임시 래핑 함수
        추후 입력 바인딩 전용 파이프라인으로 전환 또는 인자를 추가로 받아 딕셔너리 분기를 통해 
        다른 스프라이트 리소스 또한 외부로 제공할 예정

2. InteractiveUIBehaviour.cs
    UI의 화면 갱신을 위한 Refresh()함수 추가
    Public) virtual void Refresh(string key = null)
        key : target 
        if 
            key is null:
        then
            target
        else
            RefreshAllComponents

3. Control.cs
    내부 자식 오브젝트의 스프라이트를 제어하기 위한 딕셔너리 필드 ControlButtons 추가
    private) Dictionary<string, Image> controlButtons;

    private) new void Awake() 함수 추가로
        오브젝트 생성시 내부 초기화 로직 추가로
    
    private) new void OnEnable()
        오브젝트 재활성화시 항상 강제 갱신