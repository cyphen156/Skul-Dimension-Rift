Branch
    설정창 UI 개발

Commit 41a42c8
[Feat] 신규 매니저 GraphicManager추가

변경 요약
    Options UI에 존재하는 설정들을 적용시키기 위한 GraphicManager를 추가하고 초기화 작업을 진행하였습니다.

    상세 수정 내용
    
    BootStrap.cs
        InitializeManagers() 수정
            신규 매니저 초기화 추가

    ResourceManager.cs
        Public) ref readOnly UserData GetUserData()함수 추가
            userData를 읽기 전용 참조반환

        Public) OptionsData GetUserOptionsData()함수 추가
            각 매니저들이 캐싱하여 동기화 할 UserData의 OptionsData를 반환합니다.
    GraphicManager.cs
        게임 그래픽 관련 설정을 제어하는 싱글턴 클래스
        
        private) Initialize() 함수 추가
            ResourceManager에서 userData.options.graphic를 받아와 내부에 캐싱

    SoundManager.cs
        private) Initialize() 함수 추가
            ResourceManager에서 userData.options.audio를 받아와 내부에 캐싱
        
    UserData.cs
        enum 클래스 전부 types로 이관

    Types.cs
        UserData 클래스에 존재하던 enum 타입들을 이관받음
        UserData의 옵션의 각각에 해당하는 설정들을 사용할 수 있습니다.
